# Copiez ce fichier en .env à la racine du projet et adaptez les valeurs.

DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=locationvoiture_db

# Obligatoire pour login / inscription / réservations (JWT)
JWT_SECRET=changez_moi_par_une_chaine_longue_et_secrete

PORT=3000
